local Workspace = game:GetService("Workspace")

local folder = Workspace:FindFirstChild("InfiniteBaseplates")
if folder then
    folder:Destroy()
    print("Baseplates reset.")
else
    print("No baseplates found.")
end
